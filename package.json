{
	"name": "harta-haine",
	"private": true,
	"workspaces": [
		"server",
		"sh-website"
	],
	"scripts": {
		"build": "npm run build --workspace=server && npm run build --workspace=sh-website",
		"netlify-build": "cd server && npm install && npx prisma generate && npm run build && cd ../sh-website && npm install && npm run build"
	}
}
